---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - ./inventory
  execution-environment:
    image: localhost/ee-zos:v1.5 
    pull:
      policy: missing
  playbook-artifact:
    enable: false
  mode: stdout
