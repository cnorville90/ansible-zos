---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - ./inventory
  execution-environment:
    image: localhost/ee-norville-main-testing:1.0
    pull:
      policy: missing
  playbook-artifact:
    enable: false
  mode: stdout
