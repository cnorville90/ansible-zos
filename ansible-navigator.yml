---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - ./inventory
  execution-environment:
    image: quay.io/cnorvill/zos_snow:v1.0 
    # image: quay.io/jacob_emery/aap4zos-ee:latest
    pull:
      policy: missing
  playbook-artifact:
    enable: false
  mode: stdout
