---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - ./inventory
  execution-environment:
    image: localhost/ee-norville-testing:v1.2 
    # image: quay.io/jacob_emery/aap4zos-ee:latest
    pull:
      policy: missing
  playbook-artifact:
    enable: false
  mode: stdout
